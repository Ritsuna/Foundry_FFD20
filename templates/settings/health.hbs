<form autocomplete="off">
  <nav class="sheet-tabs tabs">
    <a class="item" data-tab="base"><i class="fas fa-heartbeat"></i> {{localize "SETTINGS.ffd20lnrwHealthConfigTabBase"}}</a>
    <a class="item" data-tab="variants"><i class="fas fa-prescription-bottle-alt"></i> {{localize "SETTINGS.ffd20lnrwHealthConfigTabVariant"}}</a>
  </nav>

  <div class="tabbed">
  <!-- Base Tab -->
    <section class="tab" data-tab="base">
      <p class="notes">{{localize 'SETTINGS.ffd20lnrwHealthConfigInstructions'}}</p>
      <div id="hitdice">
        <label class="index table-header">{{localize 'ffd20lnrw.HitDie'}}</label>
        {{#each hitdieOptions}}
          <label class="table-header">{{this}}</label>
        {{/each}}

        {{#each hitdice as |opt hd|}}
          <label class="index">{{hd}}</label>
          <div class="switch">
            <input class="switch-check" type="checkbox" {{#if opt.auto}}checked{{/if}} name="hitdice.{{hd}}.auto" id="hitdice.{{hd}}.auto">
            <label class="switch-label" for="hitdice.{{hd}}.auto">
              <span class="switch-auto">{{localize 'SETTINGS.ffd20lnrwHealthComputeAuto'}}</span>
              <span class="switch-manual">{{localize 'SETTINGS.ffd20lnrwHealthComputeManual'}}</span>
            </label>
          </div>
          <span><input type="text" data-dtype="Number" name="hitdice.{{hd}}.rate" value="{{opt.rate}}"></span>
          <span><input type="text" data-dtype="Number" name="hitdice.{{hd}}.maximized" value="{{opt.maximized}}"></span>
        {{/each}}
        </ul>
      </div>

      <div class="form-group">
        <label>{{localize 'SETTINGS.ffd20lnrwHealthRoundingName'}}</label>
        <select name="rounding">
          {{#select rounding}}
            <option value="up">&uarr;</option>
            <option value="nearest">&#8597;</option>
            <option value="down">&darr;</option>
          {{/select}}
        </select>
        <p class="notes">{{localize 'SETTINGS.ffd20lnrwHealthRoundingHint'}}</p>
      </div>

      <div class="form-group">
        <label>{{localize 'SETTINGS.ffd20lnrwHealthContinuityName'}}</label>
        <select name="continuity">
          {{#select continuity}}
            <option value="continuous">{{localize 'SETTINGS.ffd20lnrwHealthContinuityContinuous'}}</option>
            <option value="discrete">{{localize 'SETTINGS.ffd20lnrwHealthContinuityDiscrete'}}</option>
          {{/select}}
        </select>
        <p class="notes">{{localize 'SETTINGS.ffd20lnrwHealthContinuityHint'}}</p>
      </div>
    </section>

  <div class="form-group">
    <button type="button" name="reset">
      <i class="fas fa-undo"></i> {{localize 'SETTINGS.Reset'}}
    </button>
    <button type="submit" name="submit" >
      <i class="far fa-save"></i> {{localize 'SETTINGS.Save'}}
    </button>
  </div>
</form>
